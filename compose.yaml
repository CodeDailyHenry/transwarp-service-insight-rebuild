services:
  api:
    build: .
    ports:
      - "${SLA_API_PORT:-8000}:8000"
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - >-
          import json, urllib.request;
          response = urllib.request.urlopen('http://127.0.0.1:8000/health/ready', timeout=2);
          assert response.status == 200 and json.load(response) == {'status': 'ready'}
      interval: 2s
      timeout: 3s
      retries: 10
      start_period: 2s
